%Ctrl z detiene un proceso
%bg es el comando para mandar el proceso a un segundo plano
%el caracter & seguido del comando a ejectuar lo ejecuta directamente en segundo plano
%Ctrl c termina la ejecucion del proceso en curso
%fg es el comando para regresar el proceso a primer plano
%kill es el comando para enviar señales a un proceso que identificas con su PID (visto desde el comando top), el argumento -9 funciona para terminar con el proceso
%find . -name "*.py" busca todos los archivos en el directorio actual con extension .py
%whereis es el comando para buscar la ruta de un comando o programa
%otra forma de cambiar permisos es utilizando chmod seguido de los argumentos +r +w +x -r -w -x
%metodo son las acciones que realiza el objeto
%objeto.metodo()
%un objeto es un miembro de una clase
%una clase agrupa objetos que comparten atributos

\documentclass[letterpaper, 12pt, oneside]{article}
\usepackage[utf8]{inputenc}

\title{\Huge \textbf{Resumen de clase\\08}}
\author{\textbf{Emilio López Sotelo}}
\date{\textbf{16/01/2019}}

\begin{document}
	\maketitle
\end{document}